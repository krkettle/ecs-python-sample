version: "3.7"

services: 
  api:
    container_name: "api"
    build: ./api
    tty: true
    volumes: 
      - "./api:/usr/src/app"
    command: uvicorn src.main:app --reload --host 0.0.0.0
    env_file: ./.env